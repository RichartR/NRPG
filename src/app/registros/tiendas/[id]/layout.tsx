export const revalidate = 3600;

export function generateStaticParams() {
  return [];
}

export default function TiendaDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
